# Copy this to .env.development.local and fill in your values.
# This file is safe to commit. .env.development.local is gitignored.

# URL of the FastAPI RAG backend (F3 + F4 endpoints)
DOCUSAURUS_BACKEND_URL=http://localhost:8000
